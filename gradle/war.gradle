apply plugin: "war"

bootWar {
    mainClassName = "com.neutronbinary.infectolabs.microservice.NbSolarCoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
